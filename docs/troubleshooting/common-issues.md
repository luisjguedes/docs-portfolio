# Common issues

## 400 Bad Request
**Cause:** Missing the required `name` field.  
**Fix:** Send `{ "name": "YourName" }`.

## Workflow doesn’t run
**Cause:** Workflow isn’t activated.  
**Fix:** Activate it and retry.
