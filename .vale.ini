StylesPath = styles
MinAlertLevel = suggestion

; Use a Microsoft-aligned public ruleset package (installed via `vale sync`)
Packages = Microsoft

[*]
BasedOnStyles = Vale, Microsoft, Portfolio
Vocab = Portfolio

; Lint only docs/content formats
[*.{md,mdx}]
